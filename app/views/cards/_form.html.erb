<%= form_for(@card) do |f| %>
  <% if @card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@card.errors.count, "error") %> prohibited this card from being saved:</h2>

      <ul>
      <% @card.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :issuer_id %><br />
    <%= f.collection_select :issuer_id, Issuer.all, :id, :name %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :rewards_type %><br />
    <%= f.text_field :rewards_type %>
  </div>
  <div class="field">
    <%= f.label :general_rate %><br />
    <%= f.text_field :general_rate %>
  </div>
  <div class="field">
    <%= f.label :threshold_amount %><br />
    <%= f.number_field :threshold_amount %>
  </div>
  <div class="field">
    <%= f.label :threshold_rate %><br />
    <%= f.text_field :threshold_rate %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
